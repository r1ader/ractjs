import MK from "./MK";

export default class Follower {
    constructor() {
        const _this = this
    }

    bind(config) {
        // console.log(config)
        console.log()
        MK.listen_mouse(() => {

        })
    }
}